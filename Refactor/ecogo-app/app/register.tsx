import { View, Text } from 'react-native';
import React from 'react';

const RegisterPage = () => {
	return (
		<View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
			<Text>Add input fields!</Text>
		</View>
	);
};

export default RegisterPage;